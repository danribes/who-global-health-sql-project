# Data folder

Put your raw data here.

Examples:
- original CSV files
- exported XLSX converted to CSV
- a local `.db` file if your workflow depends on it

Keep the original files untouched when possible.
Do your cleaning and transformations in the `sql/` folder, not by manually editing the source data.
